server:
  port: 8761

spring:
  application:
    name: discovery-service

eureka:
  client:
    register-with-eureka: false # 레지스트리에 유레카 서버 자신을 등록할지 여부 (default: true)
    fetch-registry: false       # 레지스트리에 있는 정보를 가져올지에 대한 여부 (default: true)